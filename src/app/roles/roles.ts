import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-roles',
  imports: [RouterLink],
  templateUrl: './roles.html',
  styleUrl: './roles.css',
})
export class Roles {}
