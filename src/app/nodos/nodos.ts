import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-nodos',
  imports: [RouterLink],
  templateUrl: './nodos.html',
  styleUrl: './nodos.css',
})
export class Nodos {}
